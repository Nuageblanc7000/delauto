{% extends "base.html.twig" %}
{% block title %}Modification du password{% endblock %}

{% block body %}
    {{ form_start(upForm) }}
    {{ form_widget(upForm) }}
    <div class="form-group">
    <button type="submit" class="btn btn-gold shadow mb-2 mt-2">modifier</button>
    <a href="{{ path('profil_user') }}"  class="btn btn-warning shadow mb-2 mt-2">Retour au profil</a>
    </div>
    {{ form_end(upForm) }}
{% endblock %}